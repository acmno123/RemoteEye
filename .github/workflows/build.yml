name: RemoteEye Multi-Platform Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-22.04]

    steps:
      - name: 📥 檢出代碼
        uses: actions/checkout@v4

      - name: 🐍 設置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 安裝套件
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter mss pillow pyautogui pyinstaller

      - name: 🔍 檔案清單確認
        shell: bash
        run: |
          echo "當前目錄路徑: $(pwd)"
          echo "檔案清單如下:"
          ls -F

      - name: 🔨 執行編譯
        shell: bash
        run: |
          # 這裡請手動輸入你真正的檔名，例如：main.py
          # 我假設你的檔名是 RemoteEye_Global.py
          TARGET_FILE="RemoteEye_Global.py"
          
          if [ -f "$TARGET_FILE" ]; then
            pyinstaller --noconsole --onefile --name "RemoteEye_v1.0.6_${{ matrix.os }}" "$TARGET_FILE"
          else
            echo "錯誤：找不到 $TARGET_FILE ！請確認檔案已 Push 到 GitHub 根目錄。"
            exit 1
          fi

      - name: 📤 儲存成果 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: RemoteEye-Release-${{ matrix.os }}
          path: dist/