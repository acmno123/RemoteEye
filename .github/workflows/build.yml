name: RemoteEye Multi-Platform Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # 更新 macOS 版本，並使用穩定版 Windows/Ubuntu
        os: [windows-2022, macos-latest, ubuntu-22.04]

    steps:
      - name: 📥 檢出代碼
        uses: actions/checkout@v4

      - name: 🐍 設置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 安裝套件
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter mss pillow pyautogui pyinstaller

      - name: 🔍 檢查檔案清單 (排錯用)
        shell: bash
        run: ls -R

      - name: 🔨 執行編譯 (PyInstaller)
        shell: bash
        run: |
          # 確保檔名與你上傳到 GitHub 的完全一致
          # 如果你的檔名其實是 remoteeye_global.py (小寫)，這裡就要改
          pyinstaller --noconsole --onefile --name "RemoteEye_v1.0.6_${{ matrix.os }}" RemoteEye_Global.py

      - name: 📤 儲存成果 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: RemoteEye-Release-${{ matrix.os }}
          path: dist/