name: RemoteEye Multi-Platform Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-22.04]

    steps:
      - name: 📥 檢出代碼
        uses: actions/checkout@v4

      - name: 🐍 設置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 安裝套件
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter mss pillow pyautogui pyinstaller

      - name: 🔨 執行編譯 (自動偵測檔名)
        shell: bash
        run: |
          # 自動尋找資料夾下最大的 Python 檔案 (通常就是主程式)
          # 或者你可以直接把下方的 FILE 換成你真正的檔名，例如 FILE="main.py"
          FILE=$(ls *.py | head -n 1)
          echo "偵測到主程式檔案: $FILE"
          
          if [ -z "$FILE" ]; then
            echo "錯誤: 找不到任何 Python 檔案！請確認你已將 .py 檔案上傳到 GitHub。"
            exit 1
          fi
          
          pyinstaller --noconsole --onefile --name "RemoteEye_v1.0.6_${{ matrix.os }}" "$FILE"

      - name: 📤 儲存成果 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: RemoteEye-Release-${{ matrix.os }}
          path: dist/